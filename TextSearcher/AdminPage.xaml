<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TextSearcher.AdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <TextBlock
                FontSize="28"
                FontWeight="SemiBold"
                Text="Admin" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Velg en eller flere mapper som søkesiden skal søke i. Undermapper tas med automatisk." />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button
                Click="AddFolderButton_Click"
                Content="Legg til mappe" />
            <Button
                Click="RemoveFolderButton_Click"
                Content="Fjern valgt" />
        </StackPanel>

        <ListView
            x:Name="FolderListView"
            Grid.Row="2"
            ItemsSource="{x:Bind SearchFolders}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="NoWrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock
            x:Name="StatusTextBlock"
            Grid.Row="3"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</Page>
